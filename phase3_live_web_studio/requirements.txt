# Core dependencies for multiagent system
streamlit==1.43.2
watchdog==3.0.0

# LLM dependencies for multiagent AI capabilities (essential only)
langchain-openai>=0.3.0
python-dotenv>=1.0.0

# Optional: Add Gemini support if needed
# langchain-google-genai>=2.0.0 